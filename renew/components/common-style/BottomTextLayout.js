import styled from "styled-components";

const BottomTextLayout = styled.div`
  font-size: 18px;
  font-weight: 600;
  letter-spacing: -0.01;
  margin: 10px 0;
  color: ${(props) => props.color};
`;

export default BottomTextLayout;
